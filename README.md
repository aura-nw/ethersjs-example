# Etherjs interaction with Zora contracts
1. Update private key in `.secret` file
2. Change the address of contracts, RPC endpoint in each file
3. Run `scripts/setup_contract.js` before using other scripts